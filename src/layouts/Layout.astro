---
// src/layouts/Layout.astro

import { ThemeProvider, createTheme } from "@mui/material";
const theme = createTheme({ /* Custom theme options */ });

---

<ThemeProvider theme={theme} client:only>
  <slot />
</ThemeProvider>